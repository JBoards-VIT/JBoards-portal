import { createTheme } from '@mui/material/styles';
let ButtonTheme = createTheme({
    palette: {
        primary: {
            main: '#75cfb8',
        },
        text: {
            primary: "#fff"
        }
    },
});

export default ButtonTheme;