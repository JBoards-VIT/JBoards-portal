import { createTheme } from '@mui/material/styles';
let TextFieldTheme = createTheme({
    palette: {
        primary: {
            main: '#75cfb8',
        },
        text: {
            primary: "#403c49"
        }
    },
});

export default TextFieldTheme;